{
  "scripts": {
    "clean": "rimraf dist/",
    "dev": "webpack --mode development --watch",
    "init": "cp asset/*.json dist/ && cp asset/parts.production.json dist/parts.json",
    "copy": "cp -R node_modules/bootstrap/dist/css/bootstrap.min.* dist/ && cp -R src/**/*.css dist/ && cp -R src/**/*.html dist/",
    "build": "npm run clean && npm run init && tsc && npm run copy"
  },
  "devDependencies": {
    "ts-loader": "^9.5.2",
    "webpack": "^5.101.3",
    "webpack-cli": "^6.0.1"
  },
  "dependencies": {
    "bootstrap": "^5.3.7",
    "rxjs": "^7.8.2"
  }
}
